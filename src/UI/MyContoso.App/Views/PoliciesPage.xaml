<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:fs="clr-namespace:FlagstoneUI.Core.Controls;assembly=FlagstoneUI.Core"
             xmlns:vm="clr-namespace:MyContoso.App.ViewModels"
             x:Class="MyContoso.App.Views.PoliciesPage"
             x:DataType="vm:PoliciesViewModel"
             Title="Policies">

    <Grid RowDefinitions="*">
        <!-- Loading indicator -->
        <ActivityIndicator IsRunning="{Binding IsLoading}"
                          IsVisible="{Binding IsLoading}"
                          VerticalOptions="Center"
                          HorizontalOptions="Center" />

        <!-- Policies list -->
        <CollectionView ItemsSource="{Binding Policies}"
                       IsVisible="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}"
                       Margin="16">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <fs:FsCard Margin="0,0,0,12">
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                            <VerticalStackLayout Grid.Column="0" Spacing="4">
                                <Label Text="{Binding Title}"
                                      FontSize="{DynamicResource FontSize.TitleMedium}"
                                      FontAttributes="Bold"
                                      TextColor="{DynamicResource Color.OnSurface}" />
                                
                                <Label Text="{Binding Category}"
                                      FontSize="{DynamicResource FontSize.BodyMedium}"
                                      TextColor="{DynamicResource Color.Primary}" />
                                
                                <Label Text="{Binding LastUpdated, StringFormat='Updated: {0:MMM dd, yyyy}'}"
                                      FontSize="{DynamicResource FontSize.BodySmall}"
                                      TextColor="{DynamicResource Color.OnSurfaceVariant}" />
                            </VerticalStackLayout>

                            <fs:FsButton Grid.Column="1"
                                        Text="View"
                                        Style="{StaticResource OutlinedButton}"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:PoliciesViewModel}}, Path=NavigateToPolicyCommand}"
                                        CommandParameter="{Binding PolicyId}"
                                        VerticalOptions="Center" />
                        </Grid>
                    </fs:FsCard>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
